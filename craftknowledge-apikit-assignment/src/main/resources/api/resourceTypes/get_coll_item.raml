#%RAML 1.0 ResourceType
get:
  description: Get the <<resourcePathName | !singularize >> with <<resourcePathName | !singularize >> Id
  responses:
    200:
      body:
        application/json:
          example:
            <<exampleReference3>>